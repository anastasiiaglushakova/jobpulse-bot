[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    -s
    --strict-markers
    --tb=short
markers =
    smoke: smoke tests
    e2e: end-to-end tests
    jobboard: tests for jobboard demo site